#include <stdio.h>

int main(void) {
	char s[50];
	int i,j,count,l;
	scanf("%s",&s);
	l=strlen(s);
	for(i=0;i<l;i++)
	{
		for(j=i+1;j<l;j++)
		{
		if(s[i]==s[j])
		{
		count++;
		}
		for(k=j)
		
		
	}
	
	return 0;
}
